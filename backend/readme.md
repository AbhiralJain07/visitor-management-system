POST   /auth/register  → Register
POST   /auth/login     → Login → Token milega

GET    /visitors       → List
POST   /visitors       → Add
PUT    /visitors/:id   → Update
DELETE /visitors/:id   → Delete

GET    /employees      → List
POST   /employees      → Add
PUT    /employees/:id  → Update
DELETE /employees/:id  → Delete

GET    /visits         → List
POST   /visits         → Add
PUT    /visits/:id     → Update
DELETE /visits/:id     → Delete

GET    /offices        → List
POST   /offices        → Add
PUT    /offices/:id    → Update
DELETE /offices/:id    → Delete
